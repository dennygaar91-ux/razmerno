export default function ConstructorViewport({state,calc}){const shelves=Array.from({length:state.shelves},(_,i)=><span key={i} className="shelf-line" style={{top:`${(i+1)*80/(state.shelves+1)+10}%`}}/>);return <main className="constructor-viewport"><div className="viewport-top"><span>🟢 Визуализация · mockup</span><span className="chip" style={{background:'rgba(232,97,44,.14)',color:'var(--orange)',borderColor:'rgba(232,97,44,.24)'}}>готово под three.js</span></div><div className="cabinet-preview" style={{width:Math.max(160,Math.min(280,state.width/8)),height:Math.max(240,Math.min(390,state.height/7))}}>{shelves}</div><div className="dim-tag w">{state.width} мм</div><div className="dim-tag h">{state.height} мм</div><div className="viewport-bottom">{[['Ширина',state.width+' мм'],['Высота',state.height+' мм'],['Глубина',state.depth+' мм'],['Деталей',calc.parts]].map(i=><div className="view-stat" key={i[0]}><small>{i[0]}</small><b>{i[1]}</b></div>)}</div></main>}
