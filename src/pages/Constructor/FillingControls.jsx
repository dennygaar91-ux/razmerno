function Stepper({label,value,onChange}){return <div className="stepper-row"><span>{label}</span><div className="stepper"><button onClick={()=>onChange(Math.max(0,value-1))}>−</button><span>{value}</span><button onClick={()=>onChange(value+1)}>+</button></div></div>}
export default function FillingControls({state,setState}){return <><Stepper label="Полки" value={state.shelves} onChange={v=>setState({...state,shelves:v})}/><Stepper label="Ящики" value={state.drawers} onChange={v=>setState({...state,drawers:v})}/><Stepper label="Штанга" value={state.bars} onChange={v=>setState({...state,bars:v})}/>{[['handles','Ручки'],['legs','Ножки'],['closers','Доводчики']].map(([k,l])=><div className="toggle-row" key={k}><span>{l}</span><button className={`toggle ${state[k]?'on':''}`} onClick={()=>setState({...state,[k]:!state[k]})}/></div>)}</>}
