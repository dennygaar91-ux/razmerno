export default function DimensionControls({state,setState}){const fields=[['width','Ширина',400,2600],['height','Высота',600,2800],['depth','Глубина',200,900]];return <div>{fields.map(([key,label,min,max])=><div className="range-row" key={key}><div className="range-label"><span>{label}</span><b>{state[key]} мм</b></div><input type="range" min={min} max={max} step="10" value={state[key]} onChange={e=>setState({...state,[key]:+e.target.value})}/></div>)}</div>}
