import {Link} from 'react-router-dom';import Button from '../../components/ui/Button.jsx';
export default function ConstructorHeader({price,type,setType}){const types=[['wardrobe','Шкаф'],['cabinet','Тумба'],['shelf','Стеллаж'],['dresser','Комод']];return <header className="constructor-header"><Link to="/" className="btn btn-ghost" style={{minHeight:36}}>← Назад</Link><Link to="/" className="logo">Размерно<em>.</em></Link><nav className="constructor-header-nav">{types.map(t=><button key={t[0]} className={`constructor-tab ${type===t[0]?'active':''}`} onClick={()=>setType(t[0])}>{t[1]}</button>)}</nav><div style={{display:'flex',alignItems:'center',gap:10}}><div className="constructor-price-wrap" style={{textAlign:'right'}}><b style={{fontFamily:'var(--fm)',fontSize:20}}>{price}</b><small style={{display:'block',color:'var(--text-muted)'}}>предв. стоимость</small></div><Button to="/auth">Оформить заказ</Button></div></header>}
