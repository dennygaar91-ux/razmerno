export default function TrustBar(){const items=['8–14 дней от замера до доставки','Точность ±1 мм на каждой детали','Проверка перед запуском','Доставка по всей России'];return <section style={{padding:'26px 0',background:'var(--bg-soft)',borderBlock:'1px solid var(--border)'}}><div className="container" style={{display:'flex',gap:'20px',justifyContent:'space-between',flexWrap:'wrap'}}>{items.map(x=><div className="chip" key={x}>● {x}</div>)}</div></section>}
