import {useState} from 'react';import SectionHeader from '../../components/ui/SectionHeader.jsx';import {faq} from '../../data/faq.js';
export default function FAQSection(){const[open,setOpen]=useState(0);return <section className="section" id="faq" style={{background:'var(--bg-soft)'}}><div className="container"><SectionHeader kicker="Вопросы" title="Снимаем главные риски"/><div style={{display:'grid',gap:8}}>{faq.map((f,i)=><article className="card" key={f.q}><button onClick={()=>setOpen(open===i?null:i)} style={{width:'100%',padding:'20px 26px',display:'flex',justifyContent:'space-between',fontWeight:800,fontFamily:'var(--fh)',fontSize:16,textAlign:'left'}}>{f.q}<span>{open===i?'×':'+'}</span></button>{open===i&&<p className="lead" style={{padding:'0 26px 24px'}}>{f.a}</p>}</article>)}</div></div></section>}
