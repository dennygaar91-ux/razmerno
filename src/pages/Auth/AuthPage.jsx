import {useState} from 'react';import {Link} from 'react-router-dom';import AuthHero from './AuthHero.jsx';import LoginForm from './LoginForm.jsx';import RegisterForm from './RegisterForm.jsx';
export default function AuthPage(){const[mode,setMode]=useState(new URLSearchParams(location.search).has('register')?'register':'login');return <main className="auth-page" style={{display:'grid',gridTemplateColumns:'1fr 1fr',minHeight:'100svh'}}><AuthHero mode={mode}/><section style={{display:'flex',flexDirection:'column',justifyContent:'center',padding:'clamp(28px,5vw,56px)',background:'#fff'}}><Link to="/" style={{color:'var(--text-muted)',fontWeight:700,marginBottom:40}}>← На главную</Link>{mode==='login'?<LoginForm switchMode={()=>setMode('register')}/>:<RegisterForm switchMode={()=>setMode('login')}/>}</section></main>}
