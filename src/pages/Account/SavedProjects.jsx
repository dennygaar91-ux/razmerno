import {useNavigate} from 'react-router-dom';import {savedProjects} from '../../data/orders.js';
export default function SavedProjects(){const nav=useNavigate();return <div><h3 style={{marginBottom:14}}>Ваши проекты</h3><div className="card">{savedProjects.map(p=><div className="order-row" key={p.name} onClick={()=>nav('/constructor')} style={{gridTemplateColumns:'1fr auto'}}><div><b>{p.name}</b><p className="card-text" style={{margin:0,fontFamily:'var(--fm)'}}>{p.dims}</p><small>{p.date}</small></div><span>→</span></div>)}<button className="order-row" style={{width:'100%',gridTemplateColumns:'1fr',color:'var(--orange)',fontWeight:800}}>+ Новый проект</button></div></div>}
