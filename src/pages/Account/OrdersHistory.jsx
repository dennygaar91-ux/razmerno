import {useNavigate} from 'react-router-dom';import {orders} from '../../data/orders.js';
export default function OrdersHistory(){const nav=useNavigate();return <div><h3 style={{marginBottom:14}}>История заказов</h3><div className="card">{orders.slice(1).map(o=><div className="order-row" key={o.id} onClick={()=>nav(`/account/order/${o.id}`)}><img src={o.img}/><div><b>{o.name}</b><p className="card-text" style={{margin:0,fontFamily:'var(--fm)'}}>{o.dims}</p><small>Заказ №{o.id} от {o.date}</small></div><span className={`status ${o.status==='Доставлен'?'done':''}`}>{o.status}</span></div>)}</div></div>}
