export type StaticPageKey = "home" | "measurements" | "materials" | "assembly" | "constructor";

/**
 * StaticDesignPages was split into lazy page modules in src/static-pages.
 * This file is intentionally kept as a small compatibility marker.
 */
