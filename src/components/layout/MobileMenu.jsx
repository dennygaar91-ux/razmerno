import {Link} from 'react-router-dom';import {navigation} from '../../data/navigation.js';export default function MobileMenu({open,onClose}){return <div className={`mobile-menu ${open?'open':''}`}>{navigation.map(n=><Link key={n.label} to={n.to} onClick={onClose}>{n.label}</Link>)}<Link to="/auth" onClick={onClose}>Личный кабинет</Link><Link className="btn btn-primary" to="/constructor" onClick={onClose}>Рассчитать мебель</Link></div>}
