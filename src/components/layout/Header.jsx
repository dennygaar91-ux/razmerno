import {useState} from 'react';import {Link} from 'react-router-dom';import {navigation} from '../../data/navigation.js';import Button from '../ui/Button.jsx';import MobileMenu from './MobileMenu.jsx';
export default function Header(){const[open,setOpen]=useState(false);return <><header className="site-header scrolled"><div className="container header-inner"><Link to="/" className="logo">Размерно<em>.</em></Link><nav className="desktop-nav">{navigation.map(n=><Link key={n.label} to={n.to}>{n.label}</Link>)}</nav><div className="header-actions"><Link to="/auth" className="profile-ic">👤</Link><Button to="/constructor">Рассчитать мебель</Button><button className="burger" onClick={()=>setOpen(!open)}>☰</button></div></div></header><MobileMenu open={open} onClose={()=>setOpen(false)}/></>}
