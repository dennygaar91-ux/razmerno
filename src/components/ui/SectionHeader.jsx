export default function SectionHeader({kicker,title,lead,dark=false}){return <div className="section-header reveal" style={{textAlign:'center',marginBottom:'clamp(36px,5vw,56px)'}}>{kicker&&<span className="kicker">{kicker}</span>}<h2 className="h-lg" style={{marginTop:kicker?18:0,color:dark?'#fff':undefined}} dangerouslySetInnerHTML={{__html:title}} />{lead&&<p className="lead" style={{margin:'18px auto 0',maxWidth:620}}>{lead}</p>}</div>}
